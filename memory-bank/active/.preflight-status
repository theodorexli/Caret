PASS WITH ADVISORY

Level 2 confirmed in tasks.md and progress.md. Planning checkboxes through Pre-Mortem are complete. Unrelated leftover creative-supabase-company-control-plane.md was not treated as a design input for this task.

TDD Plan Encoding: units 1 and 2 are executable and list stub tests → stub interface → red tests → green code. Unit 3 is prose/policy (README + techContext) and owes no tests. Unit 4 is labeled executable but is Build live verification with no committed files; under What TDD Governs it is not an implementable slice, so missing XCTest steps do not fail. No scheduled change-detector was struck. No test/production swap was required.

Convention: pin JSON + Python unittest, ScreenpipeSupervisor enum + CaretTests, lease/plist under ignored .local/, start/stop signatures unchanged. Matches current layout. Package.swift still has no test target; CaretTests in the Xcode project is the existing supervisor suite.

Dependency: CaretApp only calls start/stop. Python last-N and test_screenpipe_history read lease fields and load_pin metadata, not launch[0]. check_sources does not inspect launch argv. Pin-byte checksum will change; the next healthy run rewrites the lease.

Conflict: this replaces Process + /usr/bin/env, it does not duplicate an existing launchd helper. Public lease schema and adopt pid 0 stay.

Completeness: requirements 1–5 and 7 map to units 1–2 plus the live check. Requirement 6 (feature branch) has no plan step.

Advisories for Build (plan acceptable as-is):

1. Resolve must not execute the obtain string. obtain is `npx -y screenpipe@0.4.50`, which runs the CLI (and can bring up a default :3030). resolveBinary(obtain:name:) plus “after obtain” can be read as Process of that string — the Caret-child npx this task exists to remove. Resolve in one shot with `npx -y --package screenpipe@0.4.50 which screenpipe` (pin package + version + launch[0]). Obtain stays pin metadata. Short-lived npx for `which` is fine; `/usr/bin/env` + obtain tokens is not.

2. ownsJob must be set when the job is bootstrapped, before waitForHealth — same moment as today’s `process = child`. start() uses try? run(), so a health timeout is swallowed. If ownsJob is set only after health, stop() will not bootout and the next run sees a taken port and cannot respawn. Bootout on health failure, or leave the flag set.

3. The spawn `run` XCTest cannot succeed as a happy-path return: a free port means waitForHealth throws (60s) unless health is skipped, and resolveBinary would shell out to real npx. Inject a resolved file URL, skip or expect notHealthy, and do not resolve on the adopt path so existing tests stay offline. Gate bootout as well as bootstrap.

4. Live-check step 4.4’s fallback (launchctl submit/bootstrap of the Developer ID Mach-O) is already unit 2. If Caret is still in the TCC chain, there is no second design — report blocked. Do not set AssociatedBundleIdentifiers to Caret.

5. Requirement 6 (feature branch) is not a numbered step. Create or use one before Build.

Radical innovation (advisory, not applied): do not bootstrap from Caret. The progress note already says a non-child launch of this binary got screen/accessibility true, and today’s Caret child did not. Unit 2 hopes Caret-initiated launchctl is different from Caret-initiated Process. Structural sketch: write ~/Library/LaunchAgents/dev.caret.hackathon.screenpipe.plist with ProgramArguments = resolved Mach-O + record flags; Caret never calls bootstrap. If :3031 is free, Teddy’s onboarding (or a printed one-liner) runs `launchctl bootstrap gui/$UID` from that already-proven outside context; Caret only waitForHealth + lease (same as adopt). stop() default is adopt-only so quit never kills the recorder. That matches AC 6 instead of betting bootstrap attribution.
