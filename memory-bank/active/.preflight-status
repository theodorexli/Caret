PASS WITH ADVISORY

Preflight run 2026-09-19 against the replanned Level 4 milestone list for caret-pinned-screenpipe (post launch-and-supervise override).

All Level 4 checks passed:
- Prerequisites: progress.md Complexity is Level 4; milestones.md exists; header matches the tasks.md task id.
- Checklist shape: four GFM checkbox lines under Execution Order; no stray checkboxes elsewhere.
- Coverage: brief requirements 1-3 and acceptance criteria 1-4 all map to milestones (pin -> M1, facade/hard-fail -> M2, launch/supervise -> M3, docs -> M4). No overlap.
- Scope: M1 L2, M2 L2, M3 L3, M4 L1; each independently deliverable and concrete.
- Order: serial walk M1-M2-M3-M4 is safe (M2 may use a fixture lease until M3 lands); the Mermaid DAG agrees with the checklist.
- Invariants: section exists; handoff rules stated for both shared artifacts (pin: only M1 may change it; lease: M1 defines fields, M3 writes the live lease, M2 consumes only, no port-3030 rediscovery).
- Done and risks: every checkbox has a matching heading block with a judgeable Done and Risks line.

Advisory findings:
1. Radical innovation candidate: make the launcher lease a versioned, machine-checked contract in M1 (JSON schema or golden fixture plus a small contract test in the Python suite). M2's client validates against it; M3's Done requires the Swift-written lease to pass the same contract. This turns the riskiest cross-milestone seam (Swift producer / Python consumer) from prose into a test and makes the M2||M3 parallelism enforceable rather than hopeful. Advisory only; the plan is valid without it.
2. Minor: the lease handoff rule is distributed across invariant 7 and the M1/M3 Done blocks. A single explicit line in Cross-milestone invariants ("M3 writes the lease; M2 reads it; only M1 may change its field list") would make the rule unmissable. Not required.
