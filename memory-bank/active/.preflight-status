PASS WITH ADVISORY

Preflight run 2026-09-19 (second pass), task caret-screenpipe-launch-if-port-free (Level 2). Re-plan resolves the prior FAIL (fixable): unit 1's behavior list now names "Adopted healthy listener writes the lease with sentinel pid 0, leaves process nil" and "Stale lease is replaced on adopt", both with planned tests; leasePayload's existing pid: Int parameter is unchanged, so the sentinel needs no interface change. Verified against the codebase:

1. Wrong-reference guard: progress.md Complexity is Level 2, correct reference used. Pass.
2. Prerequisites: tasks.md status shows planning, test planning, implementation plan, tech validation, and pre-mortem all complete; branch feat/caret-screenpipe-launch-if-port-free is checked out (confirmed via git). Pass.
3. TDD Plan Encoding: unit 1's four steps are ordered stub tests → stub interface → write tests/run red → write code/run green, matching the mandated process. The two new behaviors resolve the prior gap and are genuine executable-behavior tests (not change-detectors). Units 2-3 stay prose/policy (Info.plist/pbxproj build config and docs); asserting generated Info.plist bytes would be a change-detector, so the manual plutil check in unit 2 step 3 is the right verification, not a unit test. Pass.
4. Convention Compliance: ScreenpipeSupervisor.swift is `enum` with `static func`s; the three new functions (port(from:), isListening(port:), shouldSpawn(port:)) match that shape. Test file apps/mac/Tests/ScreenpipeSupervisorTests.swift already follows `final class …Tests: XCTestCase` / `test…` naming; none of the six new test names collide with existing tests. Pass.
5. Dependency Impact: confirmed CaretPaths.projectRoot (SkillRepository.swift) also backs SkillRepository.skillsRoot, and MemoryRepository/CaretNote read the same accessor - unit 3's docs step now explicitly calls this out ("supervisor, skills, memories, notes"), closing the previous advisory. Pass.
6. Conflict Detection: no existing port-listen probe (`NWListener`, `bind(`, etc.) elsewhere in apps/mac/Sources/Caret - no duplication. `stop()` stays `process?.terminate()`; since skip-spawn never assigns `process`, an adopted foreign listener is never terminated by Caret quit - the ownership contract from the project brief holds. Existing test `testLeaseURLAndPinEndpoint` still exercises `leasePayload(pid: 9, ...)`, compatible with a sentinel-0 caller elsewhere. Pass.
7. Completeness: confirmed caret/screenpipe.py's `_require_health` reads only `endpoint` and `expected_version` from the lease, never `pid` - so a sentinel pid of 0 cannot trip the Python last-N hard-fail path. All four project-brief requirements and acceptance criteria map to a plan unit and file list. Pass.

Advisory (radical innovation, non-blocking): ScreenpipeSupervisor's isListening/shouldSpawn/adopt-vs-spawn/stale-lease-delete logic is written as one-off, Screenpipe-specific code. systemPatterns.md already names four more selected upstreams (KeyType, GhostType, Computer Use Jev, Skyvern) that Caret.app does not yet launch. When one of those gets a supervisor, the same "launch-if-port-free, adopt-if-healthy, never kill what you didn't spawn" shape will need reimplementing by hand unless it is factored out now. A `PinnedServiceSupervisor` generic over a small `Pin` description (launch args, health endpoint deriver, lease field list) would let a future upstream register a pin instead of writing a second supervisor. Not a requirement of this task and not a reason to change the current plan - the concrete win only materializes when a second supervisor is actually needed - but worth the operator's evaluation against the cost of an extraction pass now versus later.
