PASS WITH ADVISORY

Checked Level 2 plan `caret-debug-last-n-preview` against `caret/screenpipe.py`, `caret/__main__.py`, `tests/test_screenpipe_history.py`, `StatusBarController`, `AppDelegate.showSettingsWindow`, `ScreenpipeSupervisor`, `CaretPaths`, `Caret.xcodeproj` / `CaretTests`, `apps/mac/Package.swift`, and README. Leftover `memory-bank/active/creative/creative-supabase-company-control-plane.md` was ignored as out of scope.

Wrong-reference: Complexity is Level 2, not Level 4.

Prerequisites: init, TDD plan, implementation plan, technology validation, and pre-mortem are complete. No Level 3 creative required.

TDD encoding: Units 1–3 put stub tests, stub interface, red tests, then production code. Unit 5 is README prose and owes no tests. Unit 4 is AppKit menu/window wiring with no new tests; the testable command/format surface is in unit 3, which matches Settings / permission windows (no UI harness, do not invent XCUITest). No change-detector steps to strike. No test/production swap.

Convention: `tests/test_history_debug.py`, `caret/screenpipe.py`, `caret/__main__.py`, `apps/mac/Sources/Caret/{HistoryDebug,CaretDebugView}.swift`, `apps/mac/Tests/HistoryDebugTests.swift`, and explicit `project.pbxproj` membership match this repo. `CaretTests` compiles only the files listed on that target (today `PinnedActionsStore*` and `ScreenpipeSupervisor*`), so `HistoryDebug.swift` plus its tests must be added there as the plan already says. SPM has no test target; `make check` still will not run XCTest.

Dependency: `history-debug` must import `debug_preview` in `__main__.py`. Existing `history-*` branches stay hard-fail exit 1. Swift spawn should copy `ScreenpipeSupervisor.run` (`/usr/bin/env`, Homebrew-prefixed PATH, `currentDirectoryURL = projectRoot`) or `python3` will often miss in a launched `.app`. `debug_preview` write-code omits forwarding `lease_path` into `last_n_*`; unit 1’s tempfile lease will fail until that is passed.

Conflict: no existing debug preview or Swift Screenpipe HTTP client. Additive CLI does not change last-N inference contracts.

Completeness: Debug menu, last-2 windows / minutes / clipboard, truncated `{app, title, timestamp, text}`, per-slice `ValueError`, unchanged `history-*` hard-fail, and missing-root display all have a file/function path. Top-level JSON keys (`windows` / `minutes` / `clipboard` vs something else) are implied, not named, so Python tests and `HistoryDebug.format` could drift if written from different guesses.

Advisory — minutes glance cost (radical innovation, not applied): `last_n_minutes` hydrates `_structure` for every hit in the window (up to 200 `/frames/{id}/elements` calls) before `records[:n]`. Opening Debug can stall on a “glance.” Keep one lease/health/search path, but add optional `include_structure=False` (default True) and `record_cap` on the existing `last_n_*` functions; `debug_preview` uses `include_structure=False` and `record_cap=n` so inference callers stay unchanged and the window does not crawl accessibility for discarded rows.

Advisory — Process launch: when implementing unit 4, build `Process` like `ScreenpipeSupervisor` (`executableURL = /usr/bin/env`, same PATH prefix). `HistoryDebug.command` returning `["python3", ...]` is fine as argv after `env`.

Advisory — JSON contract: lock section keys as `windows`, `minutes`, `clipboard`, each `{ok, error, items}` with `items` as `{app, title, timestamp, text}`.

Advisory — unit 4: missing-root / process-error strings stay display-only. A `HistoryDebug.missingRootMessage` (or similar) would make that behavior list item testable without a UI harness; not required to start build.

Advisory — `last_n_minutes(n)` is a time window, not a count. `n=2` means last two minutes then cap two rows; CLI default minutes is 3. Acceptable given the brief’s “by time,” but the minutes pane is not a truncated `history-minutes` default query.
