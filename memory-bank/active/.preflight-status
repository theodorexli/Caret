PASS WITH ADVISORY

Preflight of caret-screenpipe-history (Level 3) against the pin, Python last-N client, CLI, AppDelegate, Xcode Caret/CaretTests, and live GET /health on 127.0.0.1:3030.

Wrong-reference guard passed (Complexity is Level 3). Planning checkboxes are complete. Creative was not flagged; memory-bank/active/creative/creative-supabase-company-control-plane.md is leftover from another question and is out of scope. TDD encoding passed: units 1–4 put tests before production code; unit 5 is prose/policy and owes none. No change-detector strike and no test/code swap.

Convention: new Swift sources must be added to both the Caret and CaretTests PBXSourcesBuildPhase. CaretTests has no TEST_HOST / BUNDLE_LOADER and already compiles PinnedActionsStore.swift into the test bundle; @testable import Caret will not work. SwiftPM has no test target — xcodebuild -scheme Caret test is the right command; swift build --package-path apps/mac will pick up Sources/Caret automatically. history-clipboard should grow a --lease Path flag like history-minutes/windows. Prefer CaretPaths.projectRoot over a second Bundle CaretProjectRoot lookup.

Dependency: AppDelegate.applicationDidFinishLaunching exists in apps/mac/Sources/Caret/CaretApp.swift and is the correct wire point. start() must not wait for npx/health on the main thread or launch freezes. /health has no pid field; lease_fields still require pid — write the child pid on spawn, and on attach any present integer is enough because load_lease only checks keys. Existing test_minutes_returns_records uses one row, so dropping reverse will not go red until the new order cases land.

Conflict: no ScreenpipeSupervisor or lease writer exists. Attach-if-healthy matches this machine: unauthenticated /health is version 0.4.50 and healthy, launchd com.screenpipe.agent owns 3030, ui_recorder.clipboard_capture is false. The plan will write a lease and skip spawn; acceptance criterion 2 (live /health reports clipboard capture on) will fail until that leftover recorder is replaced. The planner already chose honest-empty history-clipboard over fighting launchd. Do not invent a port scan; do not treat CaretPaths or load_pin as something to rewrite.

Completeness: requirements 1–6 have files and steps (pin flag, drop reverse, last_n_clipboard + history-clipboard, Swift spawn-or-attach + lease, README line). Clipboard query shape is real: GET /search?content_type=input&order=descending, keep content.event_type == clipboard, map content.text_content / app_name / timestamp. Use a limit like the existing 200 (or paginate); limit=N on mixed input events can return fewer than N clipboard rows and hard-fail while more copies exist. Pin path caret/screenpipe_pin.json and endpoint http://127.0.0.1:3030 are implied, not written in unit 4. checksum is required by the lease schema and unread by Python — any stable string is enough. No test is planned that reads ui_recorder.clipboard_capture.

Advisory — radical innovation (do not apply in this plan): put a Caret-owned port on the pin (for example 3031), add those tokens to launch, always spawn, never attach. Write that endpoint into the lease and leave com.screenpipe.agent on 3030. That makes clipboard_capture true for the process Caret actually launched, drops the attach special case, and removes the leftover-recorder empty-clipboard path. Cost is one pin field, two argv tokens, and a health wait on our port.
